/**
 * Task 4
 * 
 * Complete the code below so that the right message
 * is printed to the console if n is divisible by 3 or not
 * 
 */

const n = 9;

if (n % 3 === 0) {
    console.log(`${n} is divisible by 3`);
} else {
    console.log(`${n} is not divisible by 3`);
}